from setuptools import setup, find_packages

setup(
    name='cs188',
    version='0.1',
    author='Gianluca Gippetto',
    description='CS 188 (Artificial Intelligence) course projects',
    packages=find_packages(),
    install_requires=[],
)
